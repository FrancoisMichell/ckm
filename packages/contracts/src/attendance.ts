export enum AttendanceStatus {
  PENDING = 'pending',
  PRESENT = 'present',
  LATE = 'late',
  ABSENT = 'absent',
  EXCUSED = 'excused',
}

export enum UserRoleName {
  TEACHER = 'teacher',
  STUDENT = 'student',
}
